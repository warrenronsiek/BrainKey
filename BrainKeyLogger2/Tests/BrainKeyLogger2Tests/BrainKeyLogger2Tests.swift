import XCTest
@testable import BrainKeyLogger2

final class BrainKeyLogger2Tests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(BrainKeyLogger2().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
